import std.error;

pub fn open(path: str): *void ! Error {
    let handle: *void = null;
    if (handle == null) {
        return IOError {
            code: 1,
            message: "File not found",
            path: path,
        };
    }
    return handle;
}
