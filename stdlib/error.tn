pub error IOError {
    code: i32;
    message: *const char;
    path: *const char;
}

pub error NullError {
    code: i32;
    message: *const char;
    some_field: i32;
}

pub errors SystemDefined = {
    IOError,
    NullError,
};