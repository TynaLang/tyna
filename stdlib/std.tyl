frozen struct Array<T> {
    data: ptr<T>;
    len:  i64;
    cap:  i64;
    rank: i64;
    dims: ptr<i64>;

    // Static method (namespaced to Array)
    // static fn from_lit(l: ptr<T>, length: i64) : Array<T> {
    //     // implementation...
    // }

    // Instance method (using the 'self' convention)
    // fn join(separator: str) : str {
    //     // This is called via arr.join(" ")
    // }
}

const VERSION = "0.1.0";