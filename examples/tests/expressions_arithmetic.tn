fn main() {
    let a: i32 = 1 + 2 * 3;
    let b: i32 = (1 + 2) * 3;
    let c: i32 = 10 / 3;
    let d: i32 = 10 % 3;
    let f: f32 = 1.5f + 2.5f;
    let x: i32 = 5;
    let y: i32 = ++x + x++;
    print(a, b, c, d, f, x, y);
}
