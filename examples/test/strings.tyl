// Strings: equality, length, subscripting
let s1 = "hello";
let s2 = "world";
let s3 = "hello";
print(s1 == s2); // false
print(s1 == s3); // true
print(s1.len, s2[1]); // 5 'o'
let buf = s1.to_array();
buf[0] = 'H';
print(String::from_array(buf)); // "Hello"