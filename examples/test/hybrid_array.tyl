// New Hybrid Array Test
// arr: Array<i32> (struct with {data: ptr<i32>, len: i64, cap: i64})
// Note: We don't have array literals working yet that produce this struct, 
// so we'll need to think about how to initialize it or just test the type system/parser for now.

fn main() {
    // Current syntax T[] maps to Array<T>
    // We need to provide a way to get data into it.
    // For now, let's just see if it compiles and passes Sema.
    let arr: i32[] = [1, 2, 3]; // This should ideally create an Array<i32> with data pointing to the array and len=3
    print(arr.len);
}
