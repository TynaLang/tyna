import std.list;
import frontend.source.id;
import frontend.source.location;

choice Stmt {
    Block {
        span: SourceSpan,
        stmts: List<heap<Stmt>>,
    }
}

struct Program {
    file_id: FileId,
    statements: List<heap<Stmt>>,
}
