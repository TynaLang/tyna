import std.list;
import driver;

fn main(args: Array<str>) {
    if (args.len < 2) {
        print("Usage: tyna <file.tn>");
        return;
    }

    let file_path = args[1];
    let driver = Driver.new();
    driver.run(file_path);
}